# DXF Sketcher 1.5.3

DXF Sketcher 1.5.3 is a small hotfix release.

What changed:

- fixed the stretched `Use angle` toggle inside the Gear Generator pair preview
- refreshed the Linux release files so the published packages match this UI fix

Linux release files:

- Debian package: `dxfsketcher_1.5.3_*.deb`
- RPM package: `dxfsketcher-1.5.3-1.*.rpm`
- AppImage: `dxfsketcher-1.5.3-*.AppImage`
- AppImage update companion: `dxfsketcher-1.5.3-*.AppImage.zsync`

Notes:

- this is a focused follow-up to `1.5.2`
- Linux is still the main release target right now
